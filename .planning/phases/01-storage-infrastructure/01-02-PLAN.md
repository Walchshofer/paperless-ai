---
phase: 01-storage-infrastructure
plan: 02
type: execute
---

<objective>
Create integration tests for VisualOverlayRepository to verify all CRUD operations work correctly.

Purpose: Ensure overlay storage is reliable and tested before building features on top of it.
Output: Comprehensive test suite for overlay repository with passing tests.
</objective>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-storage-infrastructure/01-01-SUMMARY.md
@services/visual-rag/VisualOverlayRepository.js
@test/integration/README.md (if exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test utilities and fixtures</name>
  <files>test/integration/visual-rag/test-utils.js, test/integration/visual-rag/fixtures.js</files>
  <action>
  Create test utilities for overlay repository testing:

  test-utils.js:
  - createTestConnection() - connects to test database
  - cleanupTestData(docId) - removes test overlays by doc_id
  - waitForConnection(retries=5) - waits for DB to be available

  fixtures.js:
  - TEST_DOC_ID = 999999 (unlikely to conflict with real docs)
  - SAMPLE_OVERLAYS - array of test overlay objects with various labels
  - createOverlay(overrides) - factory function for test overlays

  Use ES module syntax if project uses it, otherwise CommonJS.
  Do NOT hardcode real document IDs.
  </action>
  <verify>node -e "require('./test/integration/visual-rag/fixtures')" succeeds without errors</verify>
  <done>Test utilities and fixtures load without errors</done>
</task>

<task type="auto">
  <name>Task 2: Write CRUD integration tests</name>
  <files>test/integration/visual-rag/VisualOverlayRepository.test.js</files>
  <action>
  Create comprehensive integration tests using the existing test framework (likely Jest based on package.json):

  Test cases:
  1. isAvailable() - returns true when connected
  2. saveOverlay() - saves single overlay, returns with id
  3. saveOverlays() - batch saves multiple overlays in transaction
  4. getByDocId() - retrieves all overlays for a document
  5. getByDocIdAndPage() - retrieves overlays for specific page
  6. getBySemanticLabel() - filters by label
  7. searchByOverlayData() - JSONB containment search
  8. hasOverlays() - returns true/false correctly
  9. deleteByDocId() - removes all overlays for document

  Structure:
  - beforeAll: wait for connection, clean test data
  - afterAll: clean test data, close connection
  - Each test should be independent (clean up after itself)

  Use TEST_DOC_ID from fixtures to avoid conflicts.
  Add timeout of 10000ms for connection-dependent tests.
  </action>
  <verify>npm test -- --grep "VisualOverlayRepository" passes all tests</verify>
  <done>All 9 test cases pass, no flaky tests</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Integration tests for VisualOverlayRepository with full CRUD coverage</what-built>
  <how-to-verify>
    1. Ensure PostgreSQL is running: docker ps | grep paperless_db
    2. Run integration tests: npm test
    3. Verify all tests pass including new overlay tests
    4. Check test output shows overlay operations working
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Test utilities load without errors
- [ ] Fixtures provide valid test data
- [ ] All 9 CRUD test cases implemented
- [ ] `npm test` passes with overlay tests included
- [ ] No test pollution (cleanup works correctly)
</verification>

<success_criteria>
- All integration tests pass
- Test coverage includes: save, get, delete, search, hasOverlays
- Tests are isolated and don't affect production data
- Phase 1 complete - storage infrastructure ready
</success_criteria>

<output>
After completion, create `.planning/phases/01-storage-infrastructure/01-02-SUMMARY.md`

Since this is the final plan for Phase 1, also update ROADMAP.md:
- Mark Phase 1 as complete
- Update progress table
</output>
