---
phase: 01-storage-infrastructure
plan: 01
type: execute
---

<objective>
Establish PostgreSQL connectivity from both Docker containers and Windows host for Visual RAG overlay storage.

Purpose: Enable overlay storage to work in all environments (development, testing, production).
Output: Working PostgreSQL connection with schema migrated and accessible from host.
</objective>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@services/visual-rag/VisualOverlayRepository.js
@migrations/init_council_storage.sql
@config/config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose PostgreSQL port in docker-compose</name>
  <files>C:/Users/pwalc/MyApps/paperless-ngx/docker-compose.yml</files>
  <action>
  Add port mapping "5432:5432" to the db service in paperless-ngx docker-compose.yml.
  This allows Windows host to connect to PostgreSQL for development and testing.

  Find the db service section and add:
  ```yaml
  ports:
    - "5432:5432"
  ```

  Do NOT remove any existing configuration. Only add the ports section.
  </action>
  <verify>grep -A 5 "db:" docker-compose.yml shows ports: - "5432:5432"</verify>
  <done>PostgreSQL port 5432 exposed to host</done>
</task>

<task type="auto">
  <name>Task 2: Update VisualOverlayRepository for dual-mode connectivity</name>
  <files>services/visual-rag/VisualOverlayRepository.js</files>
  <action>
  Update the initPool() function to support both Docker internal and host access:

  1. Add POSTGRES_HOST environment variable check with fallback chain:
     - POSTGRES_HOST (explicit override)
     - PAPERLESS_DBHOST (paperless convention)
     - 'localhost' for host access (NOT 'db' which only works in Docker)

  2. Add connection retry logic with 3 attempts and 1 second delay

  3. Add better error messages that indicate which host/port failed

  Keep lazy-loading behavior. Do NOT change the class API.
  </action>
  <verify>node -e "const {visualOverlayRepository} = require('./services/visual-rag'); visualOverlayRepository.isAvailable().then(console.log)"</verify>
  <done>Repository connects from both Docker (db:5432) and host (localhost:5432)</done>
</task>

<task type="auto">
  <name>Task 3: Fix migration to remove FK constraint</name>
  <files>migrations/init_council_storage.sql</files>
  <action>
  Modify the migration to work standalone without FK to paperless-ngx documents table:

  1. Remove "REFERENCES documents(id) ON DELETE CASCADE" from doc_id column
  2. Keep doc_id as BIGINT NOT NULL (we still reference paperless docs by ID)
  3. Add a comment explaining the logical relationship without enforcing FK
  4. Keep all existing indexes and constraints

  Reason: paperless-ai runs as a separate application and may not have direct
  access to enforce FK constraints on the paperless-ngx database. The application
  layer handles document lifecycle.

  Also create a helper script to run the migration:
  - migrations/run-migration.js that reads the SQL and executes it
  </action>
  <verify>Run migration script and verify table created with: SELECT * FROM visual_overlays LIMIT 1</verify>
  <done>Migration runs successfully, visual_overlays table exists without FK constraint</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] docker-compose.yml has port 5432 exposed
- [ ] Can connect to PostgreSQL from Windows: `psql -h localhost -U paperless -d paperless`
- [ ] Migration script runs without errors
- [ ] visual_overlays table exists with correct schema
- [ ] VisualOverlayRepository.isAvailable() returns true from host
</verification>

<success_criteria>
- PostgreSQL accessible from both Docker network and Windows host
- Migration runs successfully creating visual_overlays table
- No FK constraint issues preventing standalone operation
- Repository connects and reports available
</success_criteria>

<output>
After completion, create `.planning/phases/01-storage-infrastructure/01-01-SUMMARY.md`
</output>
