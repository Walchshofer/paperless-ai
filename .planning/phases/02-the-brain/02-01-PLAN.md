---
phase: 02-the-brain
plan: 01
type: execute
---

<objective>
Implement a LocalTranslator module to provide deterministic local translation for prompt and response alignment.

Purpose: Enable language alignment between user input and expert model native language without relying on external APIs.
Output: LocalTranslator module, config wiring, exports, and tests.
</objective>

<execution_context>
@~/.codex/skills/skills/create-plans/workflows/execute-phase.md
@~/.codex/skills/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@config/config.js
@services/aiServiceFactory.js
@services/ollama/index.js
@services/experts/ExpertPipelineExecutor.js
@services/experts/index.js
@test/integration/expert-pipeline.test.js
@test/fixtures/mocks.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LocalTranslator implementation and config</name>
  <files>services/experts/translation/LocalTranslator.js, services/experts/translation/index.js, config/config.js</files>
  <action>
Create a LocalTranslator class with a translate(text, sourceLang, targetLang, options) method that uses the local Ollama service (ollamaService.chat or generate) with a strict translation system prompt. Add a fast path for same-language inputs to return the original text. Normalize empty/short input to return early. Add a config block (e.g., config.translation) with model name, timeout, and maxTokens defaults so translation can be tuned without code changes. Avoid external API calls and avoid auto-detecting language inside the translator to keep routing decisions deterministic and testable.
  </action>
  <verify>npm test -- --grep "LocalTranslator"</verify>
  <done>LocalTranslator returns translated text via local Ollama, honors same-language fast path, and reads defaults from config.</done>
</task>

<task type="auto">
  <name>Task 2: Wire translator exports and add focused tests</name>
  <files>services/experts/index.js, services/experts/ExpertPipelineExecutor.js, test/integration/expert-pipeline.test.js, test/fixtures/mocks.js</files>
  <action>
Export LocalTranslator from services/experts/index.js and add optional translator injection in ExpertPipelineExecutor constructor options (store but do not change behavior yet). Add tests that assert LocalTranslator calls MockOllamaService when languages differ and bypasses calls when languages match. Keep tests isolated to avoid changing existing pipeline test expectations.
  </action>
  <verify>npm test -- --grep "LocalTranslator"</verify>
  <done>Translator is exported, injectable, and covered by tests without altering existing pipeline outputs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --grep "LocalTranslator"` passes
- [ ] LocalTranslator uses only local Ollama and respects config defaults
</verification>

<success_criteria>
- LocalTranslator module exists with deterministic translation behavior
- Config includes translation defaults
- Tests validate translation calls and bypass behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-brain/02-01-SUMMARY.md`
</output>
