---
phase: 02-the-brain
plan: 02
type: execute
---

<objective>
Introduce a cost-aware SemanticRouter that selects pipelines and models based on confidence and cost signals.

Purpose: Improve routing efficiency by balancing model quality against cost and confidence thresholds.
Output: SemanticRouter module, config tuning, integration in ExpertRegistry, and tests.
</objective>

<execution_context>
@~/.codex/skills/skills/create-plans/workflows/execute-phase.md
@~/.codex/skills/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@config/config.js
@services/experts/ExpertRegistry.js
@services/experts/ExpertPipelineExecutor.js
@services/experts/pipelines/models.js
@services/prompts/PromptRegistry.js
@services/utils/modelResolver.js
@test/integration/expert-pipeline.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SemanticRouter module and config</name>
  <files>services/experts/routing/SemanticRouter.js, services/experts/routing/index.js, config/config.js</files>
  <action>
Create a SemanticRouter that accepts classification results and candidate pipelines, scores them using confidence thresholds, domain alignment, and a configurable cost-weight table. Add a config block (e.g., config.semanticRouter) with minConfidence, costWeights per model tier, and an enable flag. Keep the router deterministic: no randomization and no external calls. Avoid duplicating ExpertRegistry logic; the router should use existing pipeline metadata and only adjust selection based on cost/quality rules.
  </action>
  <verify>npm test -- --grep "SemanticRouter"</verify>
  <done>SemanticRouter can rank pipelines deterministically using confidence + cost weights with config-driven defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate SemanticRouter into routing flow and add tests</name>
  <files>services/experts/ExpertRegistry.js, services/experts/ExpertPipelineExecutor.js, test/integration/expert-pipeline.test.js</files>
  <action>
Wire SemanticRouter into ExpertRegistry.route so that when enabled it selects the pipeline before falling back to the current documentType/domain logic. Preserve existing routing behavior when disabled. Add tests that validate cost-aware selection (e.g., high confidence chooses expert model, low confidence falls back to general) without changing unrelated tests.
  </action>
  <verify>npm test -- --grep "SemanticRouter"</verify>
  <done>SemanticRouter is used when enabled, fallback behavior remains unchanged, and tests cover routing outcomes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --grep "SemanticRouter"` passes
- [ ] Routing behavior unchanged when semantic routing disabled
</verification>

<success_criteria>
- Cost-aware routing is configurable and deterministic
- ExpertRegistry uses SemanticRouter when enabled
- Tests validate selection logic and fallbacks
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-brain/02-02-SUMMARY.md`
</output>
