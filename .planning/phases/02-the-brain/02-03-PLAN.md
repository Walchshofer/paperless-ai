---
phase: 02-the-brain
plan: 03
type: execute
---

<objective>
Create a TemplateRegistry for localized prompt templates and migrate hardcoded prompts into it.

Purpose: Centralize prompt localization so expert models receive prompts in their native language while preserving intent-specific structure.
Output: TemplateRegistry, TemplateManager, localized template variants, PromptFactory migration, and tests.
</objective>

<execution_context>
@~/.codex/skills/skills/create-plans/workflows/execute-phase.md
@~/.codex/skills/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@services/prompts/PromptRegistry.js
@services/PromptFactory.js
@services/prompts/MedicalPrompts.js
@services/experts/ExpertPipelineExecutor.js
@test/integration/expert-pipeline.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TemplateRegistry and migrate hardcoded prompts</name>
  <files>services/prompts/TemplateRegistry.js, services/PromptFactory.js</files>
  <action>
Implement a TemplateRegistry that stores templates by intent and language with a simple register/get API. Migrate hardcoded prompt strings from PromptFactory (medical and financial extraction prompts plus router-style system prompts) into registry entries. Update PromptFactory to fetch prompts from TemplateRegistry and fall back to existing behavior if a template is missing. Avoid changing PromptFactory method signatures or output format to prevent downstream regressions.
  </action>
  <verify>npm test -- --grep "TemplateRegistry"</verify>
  <done>PromptFactory pulls prompts from TemplateRegistry and existing prompt outputs remain stable.</done>
</task>

<task type="auto">
  <name>Task 2: Add localized variants and TemplateManager lookup</name>
  <files>services/prompts/TemplateRegistry.js, services/prompts/TemplateManager.js, test/integration/expert-pipeline.test.js</files>
  <action>
Add EN variants for Finance and Medical intents and DE variants for Chat and Summary intents. Implement TemplateManager.getTemplate(intent, lang, fallbackLang) with deterministic fallback ordering (exact lang -> fallbackLang -> any available). Add tests to validate registry lookups and fallback behavior for missing languages. Keep templates ASCII-only and avoid modifying prompt IDs used by PromptRegistry.
  </action>
  <verify>npm test -- --grep "TemplateRegistry"</verify>
  <done>TemplateManager returns localized templates for required intents with deterministic fallback and tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- --grep "TemplateRegistry"` passes
- [ ] PromptFactory outputs remain compatible with existing consumers
</verification>

<success_criteria>
- Localized templates exist for required intents (EN finance/medical, DE chat/summary)
- TemplateRegistry + TemplateManager provide deterministic selection
- Hardcoded prompts are migrated without breaking existing flows
</success_criteria>

<output>
After completion, create `.planning/phases/02-the-brain/02-03-SUMMARY.md`
</output>
