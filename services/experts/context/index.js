/**
 * Context Module Index
 *
 * Exports execution context classes for pipeline state management.
 */

const { ExecutionContext } = require('./ExecutionContext');

module.exports = { ExecutionContext };
