# Core API Framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5

# Aligned ML Stack for RTX 3090 Ti
# NOTE: Torch and flash-attn are installed in the Dockerfile using
# platform-specific wheels (CUDA build). KEEP them out of this file.
numpy==1.26.4
transformers==4.57.3
accelerate>=0.30.0

# TomoroAI & Performance
autoawq==0.2.9
auto-round==0.9.2
flash-attn==2.7.4.post1
qwen-vl-utils

# Retrieval Speed Tier
fast-plaid
fastkmeans

# Image & PDF Processing
pdf2image==1.17.0
Pillow==11.0.0
python-dotenv==1.2.1

# Vector Store (Qdrant)
qdrant-client>=1.7.0
byaldi>=0.0.7
