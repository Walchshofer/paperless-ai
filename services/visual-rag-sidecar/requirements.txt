# Core Framework
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.21
pydantic==2.12.5

# Byaldi is installed from a pinned commit in the Dockerfile to avoid
# inconsistent git-package metadata during dependency resolution.

# Aligned ML Stack for RTX 3090 Ti & ColPali-Engine
# Torch must be < 2.9.0 and Numpy must be < 2.0.0
# Dockerfile pre-installs torch==2.6.0 to match available cu124 wheels
torch==2.6.0
numpy==1.26.4
transformers==4.57.3

# Dynamic Registry Injection Dependencies (for ColQwen3 support)
# accelerate: Required for device_map and model loading
accelerate>=0.20.0

# NOTE: colpali is installed from source in the Dockerfile to ensure latest Qwen2.5/ColQwen3 definitions
# Byaldi and AWQ support (install from Dockerfile for GPU builds; also useful for dev envs)
byaldi>=0.0.7
autoawq

# NOTE: qwen_vl_utils is optional - only needed for generative Qwen3-VL models
# For embedding models (ColQwen3), it's not critical but can be added if needed
# qwen-vl-utils  # Uncomment if using generative Qwen3-VL models

# Image & PDF
pdf2image==1.17.0
Pillow==11.0.0
python-dotenv==1.2.1
